const Kodi = require('./index')
const ds = new Kodi()

describe('datasources/kodi/class', () => {
    
    
    test('sample test', () => {
        expect(true).toEqual(true);
    });

    // test('sample test', async () => {
    //     expect(await ds.getMovieList()).toEqual(true);
    // });

})